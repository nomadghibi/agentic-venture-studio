{
  "name": "agentic-venture-studio",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.7.1",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "pnpm --filter @avs/types build && pnpm --filter @avs/db build && pnpm --filter @avs/agents build && pnpm --filter @avs/api build",
    "start": "node apps/api/dist/server.js",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "format": "turbo run format"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "eslint": "^9.17.0",
    "prettier": "^3.4.2",
    "turbo": "^2.3.3",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
